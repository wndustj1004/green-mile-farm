-- =============================================================
--  그린마일 팜 — 이름 변경 기능 (10일 쿨다운)
--  profiles에 마지막 이름 변경 시각을 기록. 변경은 서버 액션(서비스롤)이
--  '이름'만 갱신하므로 별도 RLS 정책은 필요 없습니다.
--  실행 위치: Supabase 대시보드 > SQL Editor > New query > 붙여넣기 > Run
-- =============================================================

alter table public.profiles
  add column if not exists name_changed_at timestamptz;

-- =============================================================
--  끝. "Success. No rows returned"이 나오면 정상입니다.
-- =============================================================
